<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace ="com.kaisn.dao.StudentMapper">
	
	<!-- 模糊查询 -->
	<select id="selectStuInfo" resultType="java.lang.Object" parameterType="java.lang.String">
		SELECT * FROM t_student 
		<where>
			<if test="name!=null">
				name LIKE CONCAT(CONCAT('%', #{name}), '%')
			</if>
		</where>
	</select>
	
	<!-- 添加 -->
	<insert id="addEmpolyee" parameterType="com.kaisn.pojo.Student" keyProperty="no" keyColumn="no" useGeneratedKeys="true">
		SELECT * FROM t_student 
			<if test="name!=null">
				name LIKE CONCAT(CONCAT('%', #{name}), '%')
			</if>
	</insert>
	
</mapper>